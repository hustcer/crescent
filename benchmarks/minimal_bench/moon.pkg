import {
  "moonbitlang/async",
  "moonbitlang/async/http",
  "moonbitlang/async/socket",
}

// `@http.Server` and `@socket.Addr` are provided on native and wasm only.

supported_targets = "-all+native+wasm"

pkgtype(kind: "executable")
